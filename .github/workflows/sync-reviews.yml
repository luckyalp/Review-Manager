name: Stündlicher Bewertungs-Sync

on:
  schedule:
    - cron: '0 * * * *'
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Sync Reviews
        run: curl -s "https://review-manager-mu.vercel.app/api/sync-reviews"